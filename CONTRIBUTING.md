# Development workflow

- Work on a feature branch.
- Run `npm run check` before opening a pull request.
- Never put credentials or private family media in the repository.
- Keep R2 private and enforce memory visibility on the server, not only in React.
